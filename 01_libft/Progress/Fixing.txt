strchr	-> I have to cast 'c' from 'int' to 'char' so I can compare them corrrectly

strrchr	-> I have to cast 'c' from 'int' to 'char' so I can compare them corrrectly

strnstr	-> len belongs to 'big', and also if in 'len' char I haven't found the whole little (including its '\0')
	then i return NULL
	
itoa 	-> Allocating +1 byte for the sign: NOT WORK
	-> Allocating -1 byte: NOT WORK
	-> SOLUTION: check if negative, If is, allocate space for '-' too.
	Must cast it to long, because it converts -2147483648 to 2147483648 and it's not an nteger anymore
	bigges positive integer is 2147483647
	
memcpy	-> It have to crash. I shouldn't handle 'if(dst == NULL || src == NULL)'. ONLY 'if(dst == NULL && src == NULL)'

ft_split-> I have to skip any amount of separators (whith while loop) and then count any amount of char (with while loop)
	but inside that char loop, I implement an IF to check if I'm in the end of the word: 'if (s[i+1] == separator || s[i+1] == '\0')'


=============
strlcat:
- guarantee '\0' as long as size = strlen(dst) + 1
- It operates only if src and dst are NULL terminated
- It will only append at most size - strlen(dst) - 1. Because size = strlen +1 (+1 is for the '\0')
- It return the lenght of the string it tried to create, wich is equal to:
strlen(initial_dst) + strlen(src). UNLESS strlen(initial_dst) > size
then it will consider strlen(initial_dst) = size



If size < strlen(dst):
the return value should be size + len(source) (without the null terminator)
size gives you the entire buffer size of Destination in memory. Eg if you malloc(100) bytes but your destination is currently only "abc"  (+ \0) it is using 4 bytes out of 100. strlcat expects you to pass the buffer size, so 100. Then it tries to find the end of the "abc" within that 100 bytes. If it does not find one that means you are not dealing with a NUL terminated string. 
